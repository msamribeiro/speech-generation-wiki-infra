interface:
  display_name: "Speech Generation Reports"
  short_description: "Create bounded temporal research reports"
  default_prompt: "Use $speech-generation-report-agent to create a report from an immutable reconciliation snapshot."
